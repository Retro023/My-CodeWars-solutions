def string_to_array(s):
    split_string = s.split(" ")
    return split_string

# we split the input string s inot a list of words using the split() method as a delimiter,
#Then we store the result into a variable called split_string which we then return

def greet(name):
    greeting = f"Hello, {name} how are you doing today?"
    return greeting

#we use a variable called greeting to store a formated string value that replaces the {name} with whatever the name is